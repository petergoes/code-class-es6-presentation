### Array destructuring

```js
const student = [1, 'Peter', 'Goes', 7.0 ];

const [ id, firstName, lastName, grade ] = student;

console.log(id);        // 1
console.log(firstName); // "Peter"
console.log(lastName);  // "Goes"
console.log(grade);     // 7.0
```
